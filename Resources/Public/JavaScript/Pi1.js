jQuery(document).ready(function($) {

	// var historyHandler = function (){
	// 	var state = History.getState();
	// 	console.log(state);

	// }
	// History.Adapter.bind(window, 'statechange', historyHandler);

	// $('div[data-target]').each(function (){
	// 	var $el = $(this);
	// 	var target = 'div[data-target=' + $el.attr('data-target') + ']';
	// 	$('a', $el).ajaxLink({target: target});
	// });
	//

	//$('form[data-action]').submit();
	//
	//


	// $('form[name="newAddress"]').ajaxForm({
	// 	format: 'json'
	// }).bind('submit.mgn', function (event){
	// 	console.log('submit.mgn');
	// 	console.log(event);
	// });
});